// import { DiamondIcon } from '@sanity/icons'
import { defineField, defineType } from 'sanity'

export default defineType({
  name: '{{ camelCase name }}',
  title: '{{ titleCase name }}',
  type: 'document',
  // TODO: Add an icon
  // icon: DiamondIcon,
  fields: [
    defineField({
      name: 'title',
      description: 'This field appears at the top of the page',
      title: 'Title',
      type: 'string',
      initialValue: '{{ titleCase name }}',
      validation: (rule) => rule.required(),
    }),
    defineField({
      name: 'subtitle',
      description: 'This field appears under the page title at the top of the page.',
      title: 'Subtitle',
      type: 'string',
      validation: (rule) => rule.required(),
    }),
    defineField({
      name: 'seo',
      title: 'SEO & Socials',
      type: 'seo',
    }),
  ],
})
