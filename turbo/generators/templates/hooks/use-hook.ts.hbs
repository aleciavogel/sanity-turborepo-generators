import { useContext } from 'react'

import {{ pascalCase name }}Context, {
  {{ pascalCase name}}ContextType,
} from '../contexts/{{kebabCase name}}-context/context'

const use{{ pascalCase name }} = (): {{ pascalCase name }}ContextType => useContext({{ pascalCase name }}Context)

export default use{{ pascalCase name }}
